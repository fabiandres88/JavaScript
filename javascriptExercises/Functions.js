//FUNCTIONS FIRST EXERCISE CLASS 23/

/*#1. Usuario y contraseña
Solicitar al usuario usuario y contraseña para
ingresar a un sistema mediante prompt.
Crear una función para validar que que las
credenciales ingresadas sean válidas.
Definir credenciales válidas en constantes*/

/*const validuser="Fabianramirez";
const validkey="letsgo";

function validate_user (){
    var user=(prompt("Enter your user:"));
var key=(prompt("Enter your password"));
    if (user===validuser && key===validkey){
        alert ("Enter sucessful");
    }else {
        alert ("User and password incorrect")
    }
}
validate_user ();*/

//SECOND EXERCISE CLASS 23

/*#2. Diámetro del círculo
Solicitar al usuario el valor del radio de
un círculo y calcular su diámetro.
Almacenar el valor de PI en una constante*/

/*const mypi=3.1416;
function myradio (){
    var radio=Number(prompt("Please enter the circle radio:"));
    alert ("The area is: "+ (mypi*(radio*radio)));
}
myradio ();*/

//THIRD EXERCISE CLASS 23

/*#3. Pseudocódigo a javascript:
Validar si dos numeros son enteros y realizar suma-*/

/*
function validate (){
    var n1=(Number(prompt("Please enter a number:")));
    var n2=(Number(prompt("Please enter a number:")));
    if (n1>0 && n2>0){
        alert ("The add of this nambers is: "+ (n1+n2));
    }else {
        alert ("Numbers are not Int....")
    }
    }
validate ();*/

//FOURTH EXERCISE CLASS 23

/*#4. Diagrama a javascript*/

/*alert("WELCOME TO THE PROGRAM MR/MRS USER :)");
var num = (Number(prompt("Please enter a number:")));
if (num > 10) {
    var num2 = (Number(prompt("Please enter a number:")));
    if (num2 > 100) {
        document.write("The value is A");
    }
    else {
        document.write("The value is B");
    }
}
else {
    if (num < 10) {
        var num3 = (Number(prompt("Please enter a number:")));
        if (num3 > 1000) {
            document.write("The value is C");
        }
        else {
            document.write("The value is D");
        }
    }
    else {
        alert("YOUR VAULE IS WrOnG");
    }
}*/